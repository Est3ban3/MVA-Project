# Ignore the large ROOT data/split files but keep plots and other artifacts
# (summary_plots/, plots/, *.png, *.csv) tracked in git. (*.json/*.pt model
# artifacts are ignored globally - see the root .gitignore.)
*.root
*.root:Zone.Identifier
